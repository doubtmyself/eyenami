<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/mobile_navigation"
        app:startDestination="@+id/navigation_query">

    <fragment
            android:id="@+id/navigation_home"
            android:name="team.eyenami.ui.home.HomeFragment"
            android:label="@string/home"
            tools:layout="@layout/fragment_home" />

    <fragment
            android:id="@+id/navigation_query"
            android:name="team.eyenami.ui.query.QueryFragment"
            android:label="@string/query"
            tools:layout="@layout/fragment_query" />

    <fragment
            android:id="@+id/navigation_setting"
            android:name="team.eyenami.ui.settingFragment.SettingFragment"
            android:label="@string/setting"
            tools:layout="@layout/fragment_setting" />
</navigation>